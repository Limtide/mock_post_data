{
  "name": "mock-api-server",
  "version": "1.0.0",
  "description": "Serverless API for Mock Data",
  "main": "api/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "engines": {
    "node": "24.x"
  }
}